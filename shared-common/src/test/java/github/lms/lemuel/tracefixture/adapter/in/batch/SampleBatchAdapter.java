package github.lms.lemuel.tracefixture.adapter.in.batch;

/**
 * {@code ..adapter.in.batch..} 패키지 컨벤션을 모사하는 테스트 픽스처 —
 * LemuelPointcuts 의 배치 어댑터 매칭 검증 전용.
 */
public class SampleBatchAdapter {

    public String runJob() {
        return "done";
    }
}
