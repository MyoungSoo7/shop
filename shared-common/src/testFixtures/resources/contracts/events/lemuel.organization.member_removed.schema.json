{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.organization.member_removed",
  "description": "멤버십이 REMOVED(터미널)로 전이된 사실 — 그 사람은 더 이상 조직의 구성원이 아니다. 이 사실을 놓치면 퇴사자에게 발급된 권한·수단이 유효한 채로 남는다.",
  "type": "object",
  "properties": {
    "organizationId": { "type": "integer" },
    "userId": { "type": "integer" },
    "membershipId": { "type": "integer" }
  },
  "required": ["organizationId", "userId", "membershipId"],
  "additionalProperties": true
}
