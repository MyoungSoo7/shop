{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lemuel.education.course_published",
  "description": "과정이 공개된 사실. courseId 는 UUID 문자열이고 카탈로그의 orderingKey 이기도 하다 — 같은 과정의 공개/재공개 순서가 파티션 안에서 보존된다. publishedAt 은 ISO-8601 Instant(outboxObjectMapper 의 JavaTimeModule 직렬화). description 은 페이로드에 싣지 않는다 — 통지에 필요 없고, 길이 상한이 없어 메시지 크기를 예측 불가로 만든다.",
  "type": "object",
  "properties": {
    "courseId": { "type": "string", "format": "uuid" },
    "title": { "type": "string", "minLength": 1 },
    "publishedAt": { "type": "string", "format": "date-time" },
    "publishedBy": { "type": ["string", "null"] },
    "version": { "type": "integer", "minimum": 0 }
  },
  "required": ["courseId", "title", "publishedAt", "version"],
  "additionalProperties": true
}
